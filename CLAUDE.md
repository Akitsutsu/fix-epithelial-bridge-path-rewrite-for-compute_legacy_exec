@AGENTS.md

## Claude-specific notes
- Follow the repo-level rules from `AGENTS.md` as the default operating policy.
- When changing architecture, metadata semantics, or promotion logic, propose the matching `decision_log.md` and `data_contract.yaml` edits in the same patch.
- Keep this file thin; shared project rules belong in `AGENTS.md` so other coding agents can use the same guidance.
